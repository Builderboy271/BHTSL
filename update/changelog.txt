Patch v2.0.1:

- Added setting for refreshing the file explorer every time it is opened (option)
- Added caching to global search, resulting in much faster searching speed
- Truncation now occurs from the left, instead of the right
- Fixed item conditions being inverted on export
- Fixed item conditions being exported with stone sword items
- Fixed exported item files being placed in the incorrect directory